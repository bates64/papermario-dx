#pragma once
#include "npc.h"
#include "sprite/npc/Postmaster.h"

#define POSTMASTER_ANIMS \
{ \
    .idle   = ANIM_Postmaster_Idle, \
    .walk   = ANIM_Postmaster_Idle, \
    .run    = ANIM_Postmaster_Idle, \
    .chase  = ANIM_Postmaster_Idle, \
    .alert  = ANIM_Postmaster_Idle, \
    .unused = ANIM_Postmaster_Idle, \
    .death  = ANIM_Postmaster_Idle, \
    .hit    = ANIM_Postmaster_Idle, \
    .anim_8 = ANIM_Postmaster_Idle, \
    .anim_9 = ANIM_Postmaster_Idle, \
    .anim_A = ANIM_Postmaster_Idle, \
    .anim_B = ANIM_Postmaster_Idle, \
    .anim_C = ANIM_Postmaster_Idle, \
    .anim_D = ANIM_Postmaster_Idle, \
    .anim_E = ANIM_Postmaster_Idle, \
    .anim_F = ANIM_Postmaster_Idle, \
}
