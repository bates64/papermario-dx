#pragma once
#include "idle.h"

NpcSettings N(NpcSettings_Toad) = {
    .height = 30,
    .radius = 24,
    .level = ACTOR_LEVEL_NONE,
};

NpcSettings N(NpcSettings_ToadKid) = {
    .height = 23,
    .radius = 19,
    .level = ACTOR_LEVEL_NONE,
};
